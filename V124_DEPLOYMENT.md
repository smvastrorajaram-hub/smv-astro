# V126 deployment

V126 replaces the Swiss Ephemeris dependency with Astronomy Engine (MIT). No ephemeris data folder or `SWISSEPH_EPHE_PATH` is required.

Use `npm install` then `npm start` on Render. Configure Firebase, Razorpay and email secrets through Render Environment Variables.
